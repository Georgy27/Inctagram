export type AtPayload = {
  userId: string;
  userName: string;
  deviceId: string;
  iat: string;
};
export type RtPayload = {
  userId: string;
  userName: string;
  deviceId: string;
  iat: string;
};
